import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  typography: {
    fontWeight: "400",
  },
  palette: {
    white: "#fff",
    black: "#000",
    grey: "#455066",
    textHeadingColor: "#273859",
    errorColor: "#EA4C59",
    layoutTextColor: "hsla(11,80%,45%,1)",
    layoutBackgroundColor: "rgb(15 14 14 / 87%)"
  },
});
